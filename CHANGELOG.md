# Changelog

## [Unreleased]

### 新增
- 新增“同步历史”查看能力，可按股票代码、同步类型、数据源、时间范围筛选历史同步记录。
- 新增“已同步数据清理”能力，可查询单只股票当前已落库的数据概况，并按类型删除历史行情、财务数据、基础数据与实时行情展示缓存。
- 新增“同步记录”与“数据清理”之间的弱关联入口，支持从同步记录跳转到对应数据清理视图，也支持反向查看最近相关同步记录。
- 新增“快速标签”入口，可在自选股列表中直接编辑股票标签。
- 新增公开提交检查文档、公开部署脚本整理与本地辅助脚本分层说明。
- 新增针对单股同步数据源选择规则与近期关键修复的回归测试。

### 改进
- 优化单股同步数据源选择逻辑：
  - 仅同步实时行情时仅允许 `AKShare`
  - 不含实时行情时允许 `Tushare / AKShare`
  - 同时同步实时与其他数据时支持“全部 AKShare”或“实时 AKShare + 其他 Tushare”
- 优化配置管理页中厂家与数据源的密钥状态展示：
  - 数据源也支持展示 `ENV / DB` 来源
  - `API 密钥状态` 页补充数据源密钥概览与统计
  - 厂家/数据源编辑弹窗的密钥提示文案统一为“脱敏展示 + 数据库优先于环境变量”
- 优化自选股页行情刷新逻辑，按市场拆分刷新路径：
  - A 股继续走现有实时同步服务
  - 港股/美股走对应市场实时行情服务
- 优化自选股操作列布局，减少“移除”等按钮被挤到难看换行位置的情况。
- 优化标签筛选展示，增加颜色标识与“当前被多少只股票使用”的数量提示。
- 优化数据清理相关文案，将内部术语 `market_quotes` 替换为更面向用户的“实时行情展示缓存”。
- 优化公开启动脚本，采用“优先 `pnpm`、兼容 `corepack`”的执行策略。

### 修复
- 修复新增厂家时有效 `API Key / API Secret` 被后端静默清空的问题，新增与编辑现在采用一致的校验与保存逻辑。
- 修复公开示例与本地启动脚本中的默认弱口令样例，统一替换为明显占位符，避免公开提交时引发误判。
- 修复“删除已同步数据”查询接口曾出现的 `404 / 422` 问题。
- 修复港股在自选股页“有价格但无涨跌幅”的问题，港股实时行情链路现在会正确返回 `change_percent / pct_chg`。
- 修复自选股“同步实时行情”对港股错误复用 A 股实时同步链路的问题。
- 修复港股实时刷新在外部源限流时直接判失败的问题，刷新失败后会尽量回退到最近缓存。
- 修复标签删除后，自选股中仍残留已删除标签的问题。
- 修复标签管理、快速标签、自选股编辑三处标签写入逻辑不一致的问题：
  - 编辑时新增标签会同步进入标签库
  - 标签改名会同步更新已挂载股票记录
  - 单只股票标签会自动去重
- 修复中文报告里残留英文标题的问题，中文偏好下报告标题会尽量规范为中文。
- 修复部分长文本分析阶段更容易超时的问题，对部分节点增强了流式处理与错误可观测性。

### 行为变更
- 删除自选股时可同时清理该股票相关的同步历史与已同步数据。
- “删除同步记录”和“删除真实已同步数据”保持解耦：
  - 删除记录不会自动删除真实数据
  - 删除真实数据不会自动删除同步记录
- 标签删除后会同步从股票记录中移除该标签，避免出现幽灵标签。

### 测试与验证
- 补充并通过针对以下场景的回归测试：
  - 配置管理中厂家新增密钥校验、数据源密钥来源展示
  - 单股同步数据源路由规则
  - 标签去重与标签改名传播
  - 港股实时行情格式化
  - 报告中文标题规范化
- 默认后端标准套件已恢复为稳定可跑：
  - `471 passed, 2 skipped, 287 deselected`
- 前端工程校验已通过：
  - `pnpm lint`
  - `pnpm type-check`
  - `pnpm build`
